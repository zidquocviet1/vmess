<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_demo_login"
    app:startDestination="@id/qrCodeScannerFragment">

    <fragment
        android:id="@+id/qrCodeScannerFragment"
        android:name="com.mqv.vmess.ui.fragment.QrCodeScannerFragment"
        android:label="fragment_qr_code_scanner"
        tools:layout="@layout/fragment_qr_code_scanner">
        <argument
            android:name="key_scan_login"
            android:defaultValue="false"
            app:argType="boolean"
            app:nullable="false" />
    </fragment>
</navigation>